/**
 * Printify API service layer.
 *
 * Placeholder for the headless storefront integration:
 * product catalog sync, variants, pricing, and order submission.
 *
 * Env vars (to be added):
 *   PRINTIFY_API_TOKEN  — Bearer token for the Printify API
 *   PRINTIFY_SHOP_ID    — SmarTok shop id
 */

const PRINTIFY_API_BASE = "https://api.printify.com/v1";

export { PRINTIFY_API_BASE };
