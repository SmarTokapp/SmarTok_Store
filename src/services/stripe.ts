/**
 * Stripe service layer.
 *
 * Placeholder for checkout sessions and payment intent handling.
 *
 * Env vars (to be added):
 *   STRIPE_SECRET_KEY      — server-side Stripe secret
 *   STRIPE_PUBLISHABLE_KEY — client-side publishable key
 */
